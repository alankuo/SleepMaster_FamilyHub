import React, { Component } from 'react';
import '../css/App.css';
import NavBar from './NavBar'

class CreateFamily extends Component {
  render() {
    return (
      <div>
        <NavBar/>
        <p style={{padding:300+"px", fontSize:30}}>CreateFamily TBD...</p>
      </div>
    );
  }
}

export default CreateFamily;
